# Development database connection supplied by the workspace
DATABASE_URL=postgresql://user:password@host:5432/database

# Required for signing the department portal session cookie
SESSION_SECRET=replace-with-a-long-random-value