{
  "name": "cps-department-portal",
  "version": "1.0.0",
  "description": "Cyber Physical System Engineering Department Portal - Government Engineering College Thrissur",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "server": "node server/index.js",
    "client": "npm --prefix client run dev",
    "dev": "node dev.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "setup": "npm install && npm --prefix client install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
